#!/usr/bin/env sh

if [ -n "$(sed "s/.*/🔒/" /sys/class/net/tun*/operstate 2> /dev/null)" ]; then
    mullvad disconnect
    notify-send "MullvadVPN Status" "❗ Disconnected from us-sea-103!"
else
    mullvad connect
    notify-send "MullvadVPN Status" "✔️ Connected to mullvad-us182!"
fi
