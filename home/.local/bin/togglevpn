#!/usr/bin/env sh

if [ -n "$(sed "s/.*/🔒/" /sys/class/net/tun*/operstate 2> /dev/null)" ]; then
    mullvad disconnect
    notify-send "❗ Disconnected from mullvad (us-sea-103)!"
else
    mullvad connect
    notify-send "✅ Connected to mullvad (us-sea-103)!"
fi
